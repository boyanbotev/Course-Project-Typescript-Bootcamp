// TODO: json

export const config = {
    reelCount: 4, // 5
    reelLength: 4, // 4
    reelSize: 20,

    symbolSize: 165, // default 165
    topMargin: 65,

    initialBalance: 100,
    bet: 5,
};

config.symbolSize = 660/config.reelLength;